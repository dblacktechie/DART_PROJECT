double productTotal(double sum1, double sum2) {
  return sum1 * sum2;
}

void main() {
  double sum1 = 23.1;
  double sum2 = 33.2;
  double result = productTotal(sum1, sum2);
  print('the total of $sum1 and $sum2 is the $result');
}
